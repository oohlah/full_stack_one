{{> menu}}

<section class="section">
  <div class="container">
    <h1 class="title">{{category.title}}</h1>
    <div class="columns">
      <div class="column">
        {{> category-image}}
      </div>
      <div class="column">
        {{> list-placemarks}}
        {{> add-placemark}}
      </div>
   </div>
  </div>
</section>

